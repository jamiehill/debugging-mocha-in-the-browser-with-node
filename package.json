{
  "name": "debugging-mocha-in-the-browser-with-node",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "NODE_ENV=test $(npm bin)/mocha",
    "test:debug": "NODE_ENV=test $(npm bin)/mocha --source-maps --inspect --debug-brk --watch"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "enzyme": "^2.7.1",
    "jsdom": "^9.10.0",
    "mocha": "^3.2.0",
    "react-addons-test-utils": "^15.4.2",
    "react-scripts": "^0.8.5",
    "source-map-support": "^0.4.11"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  }
}
